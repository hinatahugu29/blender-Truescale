# 型紙ヘルパー カスタムシーン — 引き継ぎ準備メモ

作成: 2026-09-18
対象: 型紙ヘルパー カスタムシーン ベータ版 v1.5.6（Blender Extension）

---

## 0. この文書の位置づけ

作者（知人）から改修・保守を委任される**可能性**が出てきたため、その場合に備えて事前に整理したもの。**委任は未確定**。

レビューメモ（`レビューメモ.md`）が「何が問題か」を記録するのに対し、この文書は「**引き受けるなら何を決め、何から始めるか**」を扱う。

現時点で判明している作者側の状況:

- 販売の意図はない
- 保守管理に自信がない
- 自身での作成ではなく、生成AIを駆使して作られたもの
- 改修を委ねるかどうかは「どうしようかな」という検討段階

---

## 1. 引き受ける前に作者と決めておくこと

**技術的な話より先に、ここを詰めておく。** あとから揉めやすい順に並べた。

### 1-1. ライセンスの矛盾を解消する（最優先）

現状、2つの記述が食い違っている。

| 場所 | 記述 |
|---|---|
| `ADDON/blender_manifest.toml` | `license = ["SPDX:GPL-3.0-or-later"]` |
| `ADDON/README.txt` | 「作者の許可なく、第三者への配布・転載・再配布をしないでください」 |

GPL-3.0 は受領者に再配布の権利を与えるライセンスなので、README の再配布禁止と両立しない。またBlenderアドオン（bpyを使うもの）は慣例的にGPL互換が求められる領域でもある。

おそらくマニフェストの license 行は生成AIが埋めた既定値で、作者が意図して選んだものではない可能性が高い。

**決めること:**

- 実際にどういう扱いにしたいのか（私的利用限定 / GPLで公開 / 将来の公開可否）
- 現状の表記をどちらに合わせて直すか

※ 法的な判断が必要なら専門家に確認すること。ここでは「矛盾がある」という事実の指摘に留める。

### 1-2. 名義・著作権の扱い

`blender_manifest.toml` の maintainer が `"OpenAI / Project Contributors"` になっている。これも生成AIが入れたプレースホルダと思われ、作者名になっていない。

**決めること:**

- 改修後の maintainer / author をどう書くか
- 改修版のバージョン番号の付け方（v1.5.7 と続けるのか、fork扱いにするのか）
- 作者名をどう残すか（原作者としてクレジットするのが自然）

### 1-3. 作者の今後の関与

**決めること:**

- 完全に手を離れるのか、レビューはするのか
- 改修の方針（機能追加まで踏み込むのか、不具合修正のみか）を誰が決めるのか
- 作者自身が今後も使うのか（使うなら後方互換を気にする必要がある）

### 1-4. 引き継ぎ資産

**もらっておくもの:**

- 配布用ZIP（現状こちらには ADDON フォルダしかない。README の手順と不一致 → レビューメモ R-01）
- 過去バージョン（v1.5.0〜1.5.5 など、あれば）
- 開発中に使っていたテスト用 .blend ファイル ← **重要**
- 想定していた使い方・作りたかったもの（コスプレ造形の何か、など具体例）
- うまく動かなかった既知のケース

### 1-5. 生成AI製であることの含意

作者・こちら双方とも生成AIで書いている。これは引き継ぎ時に効いてくる。

- **「なぜこの実装なのか」を作者に聞いても答えが出ない可能性が高い。** 設計意図が人の頭の中に存在しない。
- コード内コメントは英語、READMEは日本語という混在も、その痕跡。
- 一貫性のない箇所（後述の「状態が4箇所」「登録漏れ」など）は、熟慮の結果ではなく生成の揺らぎである可能性が高い。→ **遠慮なく整理してよい**、という判断材料になる。
- テストが存在しない。**現状の挙動を先に記録しないと、直したつもりで壊す。**

---

## 2. 引き受けたら最初にやること（順番付き）

### Step 1. git 管理下に置く（最優先・15分）

**現状 git リポジトリではない。** バージョン履歴は README の文章のみ。

```
cd "E:\blender_addon\PRODUCT\型紙ヘルパー_カスタムシーン_ベータ版_v1.5.6"
git init
git add ADDON
git commit -m "作者から受領した v1.5.6 の原本"
```

**受領時点の原本を最初のコミットにする。** これをやっておかないと、「どこまでが作者の書いたコードで、どこからが自分の改修か」が二度と分からなくなる。過去バージョンをもらえたら、古い順にコミットして履歴を再構成してもよい。

### Step 2. 動作するベースラインを固定する

テストが存在しないので、**直す前に現状の挙動を記録する**。

- 単純な形状（立方体、円柱）で型紙を作り、PNG出力まで一周する
- 出力されたPNGと、その時の設定・寸法を保存しておく
- 上記に使った .blend も保存する
- これが「改修後も同じ結果が出ること」の比較対象になる

### Step 3. 未解決の調査を終わらせる

パフォーマンス問題（レビューメモ U-02/03/04, M-01〜03）は**原因未特定のまま**。`計測スクリプト4_描画速度.py` を以下3条件で実行する。

| | 条件 | 分かること |
|---|---|---|
| A | アドオン有効＋型紙あり | 現状の実測値 |
| B | アドオン有効＋型紙削除後 | 型紙データが原因か |
| C | **アドオン無効**（要Blender再起動） | このアドオンが原因か |

**Cが分岐点。** ここが決まるまで、パフォーマンス改修に着手しない。

なお、これまでに立てた仮説は以下のとおり全て決め手を欠いている。

| 仮説 | 結果 |
|---|---|
| 面数が主因 | 否定（型紙作成は実測 0.058秒） |
| 他アドオンとのdepsgraph相互作用 | 色変更では不成立 |
| コンソールへのエラー出力 | コンソールを閉じても改善せず |

### Step 4. 登録の自動化（レビューメモ C-03）

分割リファクタリングとは独立してできる。数十行、リスク低、効果大。これだけで「クラスの登録漏れ」というバグのカテゴリが丸ごと消える。

### Step 5. 既知の壊れている機能を直す

- M-03: パネルが存在しないオペレータを5つ呼んでいる（README記載の機能が5つ動かない）
- `unregister()` の消し忘れ9件（後述）

---

## 3. コードの地図

単一ファイル `ADDON/__init__.py`（11,981行）。主要な場所は以下。

### 実寸換算（この版の中核）

| 行 | 内容 |
|---|---|
| 72 | `_scene_scale_to_meters` — Scene Unit Scale の取得 |
| 95, 99 | `_mm_to_bu` / `_bu_to_mm` — 単位変換。**全ての寸法計算の土台** |
| 102 | `_world_edge_length` — matrix_world 経由の実寸 |
| 114 | `_calc_real_scale` — 全エッジの 3D長÷UV長 の**中央値**。歪みはここで平均化される |

### 展開（3D → 平面）

| 行 | 内容 |
|---|---|
| 144 | `_build_flat_mesh` — UV座標から平面Meshを生成。`flat_side_source_edge` 等のマッピングもここで作る |
| 3835 | `UNFOLDHELPER_OT_unfold_real_mesh` — UV展開の実行本体 |
| 3897 | `bpy.ops.uv.unwrap` — ANGLE_BASED、失敗時 3899 で CONFORMAL にフォールバック |

### マーキング

| 行 | 内容 |
|---|---|
| 3333 | `_pattern_seam_trails` — シームを辿って連続辺の列にする |
| 3424 | `_pattern_trail_mark_positions` — 累積長で等間隔位置を出す |
| 3495 | `_pattern_refresh_auto_notches` — 合印の全再生成（**色変更でも走る**／U-04） |
| 5779, 5792 | `_pattern_get/set_annotations` — アノテーションのJSON読み書き |
| 6750付近 | `by_source_edge` — 境界辺ごとの「接続先の島」と「内向き法線」。**縫い代やタブ生成の基盤になる**（U-09） |
| 7008 | `_pattern_auto_flat_oriented_text_items` — 型紙IDの島内配置探索（v1.5.3） |
| 7809 | `_pattern_auto_arrow_segments` — 矢印の島内配置探索（v1.5.4, v1.5.5で軽量化） |

### レイアウト・出力

| 行 | 内容 |
|---|---|
| 262 | `_get_face_islands` — 連結成分でアイランド抽出 |
| 301 | `_pack_islands` — 作成時の一列配置。**用紙は見ていない** |
| 4469付近 | `_try_shelf_layout` — 用紙を見た段組み配置。縮小は一切しない |
| 5122 | `UNFOLDHELPER_OT_export_png` — 300dpi PNG書き出し |
| 5194付近 | ワールドmm→px 変換。**オフセットがないので、ここを足すだけでタイル分割できる**（U-08） |

### 描画・キャッシュ

| 行 | 内容 |
|---|---|
| 6147 | `_pattern_invalidate_layout_cache` — epoch を進めてキャッシュ全消去＋再描画要求 |
| 8429 | `_draw_pattern_marks_3d` — GPU描画ハンドラ |
| 9113 | `_draw_pattern_text_2d` — blf描画ハンドラ |
| 504 | `_draw_paper_guide` — 用紙ガイド描画 |

### 登録

| 行 | 内容 |
|---|---|
| 11466 | `classes` タプル（**手書き。21クラスが漏れている**） |
| 末尾 | `register` / `unregister`。プロパティ定義もここに集中 |

---

## 4. 開発時に踏む落とし穴

### 4-1. エラーが見えない

`except Exception:` が105箇所、うち**70箇所が `pass` のみ**。何かが失敗しても無言で進む。

**対策:** 改修中は一時的に `pass` を `traceback.print_exc()` に置き換えて作業する。今回のパフォーマンス調査が難航した直接の原因がこれ。

### 4-2. unregister が不完全（再読み込みで壊れる）

register で 35個のSceneプロパティを作るが、unregister では24個しか消していない。**9個が消し残る。**

```
pattern_helper_arrow_mode
pattern_helper_arrow_up_axis
pattern_helper_auto_arrow_length_mm
pattern_helper_auto_island_ids
pattern_helper_correspondence_mode
pattern_helper_island_id_color
pattern_helper_island_id_size_mm
pattern_helper_island_id_style
pattern_helper_show_direction_arrow
```

開発中にアドオンを無効化→有効化すると、古い定義が残ったまま再登録される。挙動が不安定になったら、まずBlenderを再起動して切り分けること。

### 4-3. 状態が4箇所に散らばっている

| 種類 | 数 | 例 |
|---|---|---|
| モジュールグローバル | 14 | `_pattern_cache_epoch`, `_pattern_draw_cache` |
| Scene登録プロパティ | 35 | `pattern_helper_notch_color` |
| `scene["..."]` 文字列キー | 16種類 | `scene["pattern_helper_seam_source"]` |
| Objectカスタムプロパティ | — | JSON文字列（アノテーション、面マッピング） |

**どこに何があるか把握していないと、直したつもりで別経路が生き残る。** 特に②と③の並存に注意。

### 4-4. 描画ハンドラは Blender 終了まで生き続ける

`register()` で `draw_handler_add` している（11895付近）。コードを書き換えて再読み込みしても、**古いハンドラが残ることがある**。おかしくなったらBlender再起動。

### 4-5. インストール先と原本が別物

Blenderが使うのは以下のコピー。

```
C:\Users\hinat\AppData\Roaming\Blender Foundation\Blender\5.2\extensions\user_default\unfold_helper\__init__.py
```

原本（`ADDON\__init__.py`）を編集しても**そのままでは反映されない**。開発時はどちらを触るか決めて、混同しないこと（シンボリックリンクを張る、あるいは編集後に再インストールする運用にする）。

---

## 5. 既知の問題（詳細は レビューメモ.md）

### 不具合

| ID | 内容 | 状態 |
|---|---|---|
| M-03 | パネルが存在しないオペレータを5つ呼んでいる。README記載の機能が5つ動かない | **原因特定済み・未修正** |
| U-02 | 型紙作成時に重い | 原因未特定（面数説は否定） |
| U-03 | 手動レイアウトのG移動が重い | コード上の無駄は特定済み。主因かは未確定 |
| U-04 | 色変更が重い。合印は矢印の32.9倍のコスト | **因果は実測で確定**。絶対値は小さい |
| — | `unregister()` の消し忘れ9件 | 本文書 4-2 |

### 機能要望（ユーザ目線）

| ID | 内容 |
|---|---|
| U-01 | パネルから選択辺をシーム化（※既存の未登録オペレータを出すだけで済む可能性） |
| U-05 | 用紙に入らない時に情報を出す／収めて確認する |
| U-06 | 既知寸法による較正（ImageJ の Set Scale 相当）**← 設計上の欠落を埋める本命** |
| U-07 | UV展開の歪み率表示（計算済みの値を捨てているだけ） |
| U-08 | A4/A3タイル分割印刷 **← 実寸主義と衝突しない正攻法** |
| U-09 | 糊代（貼り合わせ代／縫い代・接着代） |

U-05 → U-08 → U-09 は独立した要望ではなく、**「実寸の型紙を現実の紙と素材に着地させる」一連の流れ**。U-06 はその入口を固める話。

### 保守性

| ID | 内容 |
|---|---|
| C-01 | 未登録の死にコード約1,800行（シーム作成系17クラス） |
| C-02 | 未使用関数 `_pattern_island_signature` |
| C-03 | 単一ファイル11,981行の分割。**まず「登録の自動化」だけ先行するのが推奨** |

### README

R-01〜R-05（ZIP欠落、タブ名不一致、バージョン表記、履歴配置、UV前提の未記載）

---

## 6. 改修を引き受けた場合の推奨順序

```
Step 1  git init して受領時点を固定          ← 最優先
Step 2  ベースライン（動く状態）を記録
Step 3  計測スクリプト4 で条件A/B/C を実測    ← パフォーマンスはここが出発点
Step 4  登録の自動化（C-03の一部）            ← 低リスク・高効果
Step 5  M-03 修正（壊れている5機能）
Step 6  unregister の消し忘れ修正
Step 7  except: pass の可視化（開発用）
------- ここまでが「土台を整える」フェーズ -------
Step 8  U-07 歪み率表示（数行・効果大）
Step 9  U-01 シーム化ボタンを出す
Step 10 U-05 入らない時のメッセージ改善
------- ここから機能追加 -------
Step 11 U-06 実寸較正
Step 12 U-08 タイル分割印刷
Step 13 U-09 糊代
Step 14 C-03 本格的な分割リファクタリング
```

Step 8〜10 は**どれも小さい割に体感が変わる**ので、委任直後の成果として見せやすい。

---

## 7. 手元にある資産

| ファイル | 内容 |
|---|---|
| `ADDON/` | 作者から受領した原本（未改変） |
| `レビューメモ.md` | 調査記録。全17件の所見 |
| `計測スクリプト.py` | 型紙作成のプロファイル |
| `計測スクリプト2_対話操作.py` | 色変更コールバックの比較計測 |
| `計測スクリプト3_描画解析.py` | 描画側3関数のキャッシュあり／なし計測 |
| `計測スクリプト4_描画速度.py` | ビューポート再描画速度の実測（**未実行**） |

計測スクリプトはいずれもアドオンを改変せず、読み取りと計測のみを行う。
